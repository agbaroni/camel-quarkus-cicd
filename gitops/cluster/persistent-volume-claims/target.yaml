---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: target
  namespace: camel-quarkus-tests
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: '256Mi'
  volumeMode: Filesystem
